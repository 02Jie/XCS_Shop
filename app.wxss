/* 导入style下面的 iconfont.wxss*/
@import "./styles/iconfont.wxss";
/* 在小程序中，不支持通配符样式 需要写成一下样式 */

page,view,text,swiper,swiper-item,image,navigator {
  padding : 0;
  margin: 0;
  box-sizing: border-box;
}
/*  定义变量 */
page {
  --themeColor :#ff524a;
 
}